version: '2'
services:
  registry:
    restart: always
    image: registry
    ports:
      - 5000:5000
    volumes:
      - registry-data:/var/lib/registry
volumes:
  registry-data:
    driver: local
